<div class="slide">
	<p>Workspace settings: <code>.vscode/settings.json</code> (Git repo).</p>
	<p>User settings: <code>settings.json</code> (VS Code settings sync).</p>
</div>

<style>
	.slide {
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		gap: var(--size-8);
		height: 100%;
	}

	p {
		font-size: var(--size-6);
	}

	code {
		background-color: var(--text-1);
		color: var(--surface-1);
	}
</style>
